app = "parking-guard"
primary_region = "arn"

[build]
dockerfile = "Dockerfile.fly.telemetry"

[env]
PORT = "8080"
LOG_LEVEL = "info"

[http_service]
internal_port = 8080
force_https = true
auto_stop_machines = false
auto_start_machines = true
min_machines_running = 1
processes = ["app"]

